#include<stdio.h>
#include<queue>

using namespace std;

int n, l, r;
int count = 0;
int insert[60][60];

int main(void) {

	scanf("%d %d %d", &n, &l, &r);

	for (int i = 0; i < 2; i++) {
		for (int j = 0; j < 2; j++) {
			scanf("%d", &insert[i][j]);
		}
	}

	while (1) {



		count++;
	}

	return 0;
}