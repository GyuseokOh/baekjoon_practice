#include<stdio.h>

int main(void) {

	long long int insert1, insert2;
	long long int result;

	scanf("%lld %lld", &insert1, &insert2);

	result = insert1 - insert2;
	if (result < 0) {
		result *= -1;
	}

	printf("%lld\n", result);

	return 0;
}