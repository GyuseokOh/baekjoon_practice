#include<stdio.h>

int main(void) {

	int n, result = 0;
	char insert[110];

	scanf("%d", &n);
	scanf("%s", insert);
	
	for (int i = 0; i < n; i++) {
		result += (insert[i] - '0');
	}

	printf("%d\n", result);

	return 0;
}