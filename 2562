#include<stdio.h>

int main(void) {

	int max = -1;
	int insert;
	int result;

	for (int i = 0; i < 9; i++) {
		scanf("%d", &insert);

		if (insert > max) {
			max = insert;
			result = i;
		}
	}

	printf("%d\n%d\n", max, result + 1);

	return 0;
}