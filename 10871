#include<stdio.h>

int main(void) {

	int n, x;
	int insert;

	scanf("%d %d", &n, &x);

	for (int i = 0; i < n; i++) {
		scanf("%d", &insert);
		if (insert < x) {
			printf("%d ", insert);
		}
	}
	printf("\n");

	return 0;
}