#include<stdio.h>
#include<math.h>

int main(void) {

	double a, b, c;
	double result = -1;

	scanf("%lf %lf %lf", &a, &b, &c);

	if (b < c) {
		result = floor(a/(c - b)) + 1;
	}

	printf("%d\n", (int)result);

	return 0;
}