#include<stdio.h>
#include<string.h>
#include<stdlib.h>

void push(int stack[], int *count, int num) {
	stack[(*count)++] = num;
}

int pop(int stack[], int *count) {
	if ((*count) == 0) {
		return -1;
	}
	return stack[--(*count)];
}

int main(void) {

	int n,count=0;

	scanf("%d", &n);

	int* stack = (int*)malloc(sizeof(int)*n);

	for (int i = 0; i < n; i++) {
		char insert[10];

		scanf("%s", insert);

		if (strcmp(insert, "push") == 0) {
			int num;

			scanf("%d", &num);

			push(stack, &count, num);
		}
		else if (strcmp(insert, "pop") == 0) {
			printf("%d\n", pop(stack, &count));
		}
		else if (strcmp(insert, "size") == 0) {
			printf("%d\n", count);
		}
		else if (strcmp(insert, "empty") == 0) {
			if (count == 0) {
				printf("1\n");
			}
			else {
				printf("0\n");
			}
		}
		else {
			if (count == 0) {
				printf("-1\n");
			}
			else {
				printf("%d\n", stack[count - 1]);
			}
		}

	}

	return 0;
}